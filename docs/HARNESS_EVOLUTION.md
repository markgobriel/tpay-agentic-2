# Harness evolution

| Date | Evidence | Safeguard added | Enforced by |
| --- | --- | --- | --- |
| 2026-08-17 | Initial harness approval | Explicit approval gate, validation, browser evidence, read-only verification, and immutable audit history | `AGENTS.md`, docs, backlog and controller state |
| 2026-08-17 | HR-001 verifier found the boundary check passed with no domain source and had no negative proof; follow-up verifier found it also accepted a missing/empty domain directory | Require at least one domain source module; reject imports from web/server/storage; prove both rejection paths with disposable fixtures | `scripts/architecture-check.mjs`, `tests/architecture.test.mjs` |
| 2026-08-17 | HR-002 mobile-browser full-page screenshot capture timed out while DOM evidence remained available; later viewport captures succeeded | Do not claim screenshot evidence from DOM-only validation; retry with a standard viewport capture before task completion | `backlog/tasks.json` evidence notes and verifier review |
| 2026-08-17 | The original `autonomous` command only emitted one log entry and never invoked an agent, so it could not carry the persisted work loop forward | Standalone Codex controller enforces active-state entry, emits JSONL lifecycle events, validates each completed run, supports a finite safety cap, and persists a blocker after three consecutive CLI failures | `scripts/run-autonomous.sh`, `scripts/controller-state.mjs`, `tests/controller-state.test.mjs` |
| 2026-08-17 | A Codex worker using `workspace-write` could not bind loopback TCP, which falsely blocked a task even though the controller’s host validation passed all API tests | Default workers to `danger-full-access` so required local preview and browser evidence can run; retain an explicit restricted override for non-browser work | `scripts/run-autonomous.sh`, `docs/AUTONOMY.md` |
| 2026-08-17 | A machine without `codex` on `PATH` turned the missing executable into three retried worker failures and falsely blocked the whole project | Resolve the packaged desktop Codex executable when available; fail before state mutation for an invalid explicit executable path | `scripts/run-autonomous.sh`, `tests/controller-runner.test.mjs` |
| 2026-08-17 | HR-003’s first verifier review found screenshots that did not visibly include the paused card and no durable interaction inspection record | Persist desktop/mobile all-group screenshots plus keyboard, visible-value, console, and POST/PATCH results for user-facing tasks | `.agent/evidence/HR-003/browser-checks.json`, task notes, and verifier review |
| 2026-08-17 | HR-004’s first verifier review found that completion coverage verified history and status but not the recalculated next due date | Completion acceptance tests must assert the deterministic post-action displayed date and that it changes from the prior value | `tests/routine-api.test.mjs` and fresh verifier review |
| 2026-08-17 | HR-006 browser audit found the default favicon request returned an app-originated 404, making console/network checks fail before the history flow | Explicitly suppress the unserved default favicon request until the product has a branded asset | `src/web/index.html` and `.agent/evidence/HR-006/browser-checks.json` |
| 2026-08-18 | HR-007 removal-flow audit found that cancelling an inline confirmation left focus on a hidden control and did not expose the prompt as the dialog description | Return focus to the removal trigger on cancel; expose the confirmation as a modal alert dialog with its visible prompt wired as the description | `src/web/index.html` and desktop/mobile keyboard evidence in `.agent/evidence/HR-007/browser-checks.json` |
| 2026-08-18 | HR-008 makes routine state durable, so interrupted local-file writes must not leave a partially serialized household list | Write the validated replacement list to a temporary file and atomically rename it into place | `src/storage/file-routine-repository.mjs` and the restart persistence acceptance test in `tests/routine-api.test.mjs` |
| 2026-08-18 | HR-009 novice audit found UTC serialization in the browser creation boundary, which can shift a household's routine to a neighboring calendar day near midnight | Format `createdOn` from the browser's local year, month, and day fields; reject a regression to UTC serialization | `src/web/index.html` and the offset-sensitive creation-boundary regression test in `tests/routine-api.test.mjs` |
